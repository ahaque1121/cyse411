name: DAST - ZAP Full Scan (fastbank-auth)

jobs:
  zap_scan_fastbank_auth:
    ...

    - name: Start vulnerable app
      working-directory: fastbank-auth-lab
      run: |
        npm install
        npm start &
        npx wait-on http://localhost:3003   # again, match the lab’s port

    - name: Run OWASP ZAP full scan
      with:
        target: "http://localhost:3003"
        cmd_options: "-a"
